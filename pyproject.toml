[project]
name = "mental-health-text-classifier"
version = "0.1.0"
description = "Mental health text classification using TF-IDF sklearn models"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
